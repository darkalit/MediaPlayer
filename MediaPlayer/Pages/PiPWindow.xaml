<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="MediaPlayer.PiPWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MediaPlayer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="PiPWindow">

    <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <SwapChainPanel x:Name="SwapChainPanel_Video" Grid.Row="0"/>
        <StackPanel Orientation="Horizontal" Spacing="10"  HorizontalAlignment="Center" Grid.Column="1">
            <Button Content="Back" Command="{x:Bind ViewModel.DefaultMode}"/>
            <Button>
                <Button.Content>
                    <Image>
                        <Image.Source>
                            <BitmapImage UriSource="{x:Bind ViewModel.PlayPauseIconSource, Mode=OneWay}" DecodePixelWidth="32"/>
                        </Image.Source>
                    </Image>
                </Button.Content>
            </Button>
        </StackPanel>
    </Grid>
</Window>
