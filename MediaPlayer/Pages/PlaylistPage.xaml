<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MediaPlayer.PlaylistPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MediaPlayer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ListView x:Name="ListView_Playlist"
              ItemsSource="{x:Bind Playlist}">
        <ListView.ItemTemplate>
            <DataTemplate x:DataType="local:MediaMetadata">
                <StackPanel Orientation="Horizontal" Spacing="20">
                    <TextBlock Text="{x:Bind Title}"/>
                    <TextBlock Text="{x:Bind Duration, Mode=OneWay, Converter={StaticResource DurationToStringConverter}}"/>
                </StackPanel>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</Page>
